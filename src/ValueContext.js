import React from 'react';

const ValueContext = React.createContext(45, () => {});


export default ValueContext; 


//At this moment this function is not doing anything so we re giving a default function